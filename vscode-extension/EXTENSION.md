# tapl Workflow Viewer

VS Code extension for reading tapl SQLite workflow state through `taplctl`.

## Development

```sh
npm run compile
```

Open this repository in VS Code and run the extension through an Extension Development Host.
